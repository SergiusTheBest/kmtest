#pragma once

//
// A sample calculator class that is a target for unit tests.
//

class Calculator
{
public:
    static int add(int x, int y);
    static int sub(int x, int y);
    static long long mul(int x, int y);
};